import { Line, Stage } from 'heitu';
import React from 'react';

const Index = () => {
  return (
    <div>
      <Stage>
        <Line
          // x={x}
          // y={y}
          // width={width}
          // height={height}
          // fillStyle={fillStyle}
          // strokeStyle={strokeStyle}
          // lineWidth={lineWidth}
          // border={0}
        />
      </Stage>
    </div>
  );
};

export default Index;
